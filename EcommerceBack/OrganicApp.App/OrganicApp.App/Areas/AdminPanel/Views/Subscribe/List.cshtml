@model List<Subscribe>
@{
    var count = 1;
}


<div class="row">
    <div class="col-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <h4>Subscribe</h4>

                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>
                                    User
                                </th>
                                <th>
                                    Email
                                </th>
                                <th>
                                    Created Date
                                </th>
                                <th>
                                    Message
                                </th>
                                 <th>
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                <tr>
                    <td>
                        @count
                    </td>
                    <td>
                        @(item.Email == null? "---" : item.Email)
                    </td>

                    <td>
                        <a asp-action="Delete" asp-controller="Subscribe" asp-area="AdminPanel" asp-route-id="@item.Id"><i class="bi bi-x-square"></i></a>
                    </td>
                </tr>count++;
                            }
                        </tbody>
                    </table>

                </div>
            </div>
            </div>
            </div></div>