@model List<Social>

@{
    var count = 1;
}



<div class="row">
    <div class="col-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <h4>Socials</h4>
                
                   <div class="create-btn" style="display:flex; justify-content:flex-end;">
                    <a asp-action="Create" asp-area="AdminPanel" asp-controller="Social" class="btn btn-facebook">Create</a>
                </div>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Link</th>
                                <th>
                                    Creation Date
                                </th>
                                <th>
                                    EDIT
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var dto in Model)
                            {
                <tr>
                    <td>
                        @count
                    </td>
                       
                    <td>
                        @dto.Link
                    </td>
                            
                    <td>
                        @dto.CreatedDate.ToString("MMM dd")
                    </td>
                    <td>
                                        <a asp-action="Update" asp-controller="Social" asp-area="AdminPanel" asp-route-id="@dto.Id"><i class="bi bi-pencil-square"></i></a>
                                        <a asp-action="Delete" asp-controller="Social" asp-area="AdminPanel" asp-route-id="@dto.Id"><i class="bi bi-x-square"></i></a>
                    </td>
                </tr>count++;
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>
    </div>
</div>
